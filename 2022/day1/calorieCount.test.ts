
describe("readInputFile function", () => {
    test("should successfully read input file", () => {
        // arrange
        const caloriesArray = [1, 2, 3, "", 3, 3, 3, 4, "", 2, 1, 555];
        // act
        // assert
        // expect(inputData: Number).toBe()
    })
})